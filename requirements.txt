# Web service
fastapi>=0.110
uvicorn[standard]>=0.29
APScheduler>=3.10

# Config
PyYAML>=6.0
python-dotenv>=1.0
certifi>=2024.2

# Strategy math
numpy>=1.26

# Broker + market data (paper/live). Verify latest before live use.
alpaca-py>=0.40,<1.0

# State: SQLite (stdlib) by default; Postgres when DATABASE_URL is set.
psycopg[binary]>=3.1
# (News/earnings filters use urllib — no extra dependency.)
